[package]
name = "darktide-ffi"
version = "0.3.0"
edition = "2021"
description = "C ABI exports for Darktide bundle extraction"

[lib]
crate-type = ["cdylib", "staticlib", "lib"]

[dependencies]
darktide-bundle = { path = "../darktide-bundle" }
